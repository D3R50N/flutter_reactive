name: flutter_reactive
description: "A lightweight reactive system for Flutter, inspired by simple state binding."
version: 0.0.2
homepage: "https://github.com/D3R50N/flutter_reactive"

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

